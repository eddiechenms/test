name: test
on: [push]
jobs:     
  build:
    strategy:
      matrix:
        dv: [a, b]
        cv: [c, d]
    if: matrix.dv == a && matrix.cv ==d
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - run: cat 2.txt
